<div class='default-font'>
	<div class='card d-flex flex-row bd-highlight mb-3'>
		<div>
			<h5 class='mb-2 has-text-grey'><%= activity.name %></h5>
			<small><%= activity.created_at.strftime("%d %b %Y") %></small>
		</div>
		<h6 class='has-text-weight-semibold'>$<%= activity.amount %></h6>
	</div>
</div>